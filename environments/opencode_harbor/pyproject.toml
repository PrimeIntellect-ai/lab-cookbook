[project]
name = "opencode-harbor"
description = "OpenCode v1 taskset/harness agent on Harbor tasks"
license = "MIT"
tags = ["eval", "cli_agent", "v1", "taskset", "harness"]
version = "0.1.3"
requires-python = ">=3.10"
dependencies = [
    "verifiers>=0.1.15.dev11",
    "prime-sandboxes>=0.2.10",
    "harnesses>=0.1.0.post0",
    "tasksets>=0.1.0.post0",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = ["opencode_harbor.py", "pyproject.toml", "tasks/**/*"]

[tool.verifiers.eval]
num_examples = 5
rollouts_per_example = 3

[tool.ruff]
exclude = ["tasks/**"]
