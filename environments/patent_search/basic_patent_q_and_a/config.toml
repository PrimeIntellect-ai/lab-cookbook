model = "meta-llama/Llama-3.2-1B-Instruct"
max_steps = 100
batch_size = 128
rollouts_per_example = 8

[sampling]
max_tokens = 1024

[[orchestrator.train.env]]
name = "basic-patent-q-and-a"
max_turns = 8
taskset = { id = "basic-patent-q-and-a", tools = { shared = true } }
harness = { id = "default" }
