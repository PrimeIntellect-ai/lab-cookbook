model = "Qwen/Qwen3-4B-Instruct-2507"
max_steps = 200
batch_size = 128
rollouts_per_example = 8

[sampling]
max_tokens = 2048

[[orchestrator.train.env]]
name = "advanced-patent-q-and-a"
max_turns = 8
taskset = { id = "advanced-patent-q-and-a", tools = { shared = true } }
harness = { id = "default" }
